export default async ({ store, route }) => {
  // const length = store.getters['organizations/getItemsLength']
  //
  // if (length === 0) {
  //   await store.dispatch('organizations/fetchItems', route.query)
  // }
}
